# Git 常用命令与作用说明文档

这份文档记录了我们在开发“网页小游戏（贪吃蛇与拼图）”时，所学习和使用的 Git 核心命令。它可以作为你以后找实习、做项目时的快速参考手册。

## 📦 1. 基础工作流（每天都要用）

这是 Git 最核心的三步走流程：**写代码 -> 装箱 -> 封箱存档**。

*   **`git init`**
    *   **作用**：初始化 Git 仓库。
    *   **解释**：在当前文件夹里创建一个隐藏的 `.git` 文件夹，让 Git 开始监控这个目录里的所有文件。每个项目只需要运行一次。

*   **`git add .`**
    *   **作用**：将所有修改过的文件放入“暂存区”（装箱）。
    *   **解释**：告诉 Git，“我已经修改完代码了，把当前目录下所有的改动都准备好，一会儿我要一起存档”。这里的 `.` 代表当前目录下的所有文件。

*   **`git commit -m "你的备注信息"`**
    *   **作用**：正式提交存档（封箱并贴标签）。
    *   **解释**：把你刚才 `add` 进暂存区的文件永久保存在 Git 的本地数据库中。`-m` 后面的内容必须写，用于向未来的自己或同事解释“这次存档我具体做了什么修改”。

---

## 🔍 2. 查看状态与历史（了解当前进度）

*   **`git status`**
    *   **作用**：查看当前仓库的状态。
    *   **解释**：用来检查哪些文件被修改了但还没被 `add`（红色），哪些文件已经 `add` 准备好 `commit` 了（绿色）。

*   **`git log`** / **`git log --oneline`**
    *   **作用**：查看过去的提交记录（存档记录）。
    *   **解释**：能看到你每一次 `commit` 的哈希码（比如 `f2bfe7b`）、作者、时间和备注信息。加上 `--oneline` 可以让记录显示得更简洁。

*   **`git diff`**
    *   **作用**：查看代码的具体改动（找不同）。
    *   **解释**：在你运行 `git add` 之前，可以用它来看看你到底在文件里加了哪几行（绿色 `+`）、删了哪几行（红色 `-`）。（按 `q` 退出阅读模式）。

---

## ⏪ 3. 时光倒流（吃后悔药）

*   **`git restore <文件名>`**
    *   **作用**：撤销工作区未提交的修改。
    *   **解释**：如果代码写崩了，且**还没有 `git add` 和 `git commit`**，可以用这个命令把文件恢复到上一次存档的状态。

*   **`git reset --hard <哈希码>`**
    *   **作用**：强制回退到过去的某个指定版本。
    *   **解释**：让代码完全穿越回指定的存档点。注意：这会抹掉目标版本之后的所有修改，操作需谨慎。

*   **`git reflog`**
    *   **作用**：查看所有的操作日志（包括被隐藏的未来日志）。
    *   **解释**：如果你用 `git reset --hard` 回退后后悔了，想回到“未来”，可以用这个命令找到“未来”的哈希码，然后再用 `reset` 穿越回去。

---

## ☁️ 4. 远程协作（上传到 GitHub/Gitee）

将本地代码推送到云端，不仅可以备份代码，还能方便在简历中展示你的作品。

*   **`git remote add origin <远程仓库网址>`**
    *   **作用**：将本地仓库与云端仓库绑定。
    *   **解释**：`origin` 是给远程仓库起的一个别名（默认惯例）。这行命令相当于告诉本地的 Git：“嘿，以后咱们的云端老家在这个网址”。**这个命令每个项目只需要运行一次。**

*   **`git push -u origin master`**
    *   **作用**：首次将本地代码推送到远程仓库的主分支。
    *   **解释**：把本地 `master` 分支的所有存档“发射”到云端。`-u` 的作用是记住这次的选择，以后你再想推送代码，**只需要敲 `git push` 这两个单词就可以了**，极其丝滑！

*   **`git config --global user.name "你的名字"`** / **`git config --global user.email "你的邮箱"`**
    *   **作用**：配置你的身份信息。
    *   **解释**：告诉 Git 你是谁。这是第一次安装 Git 时必须做的事情，以后提交的代码都会署上你的名字。

> **💡 终极总结（日常开发闭环）：**
> 1. 写代码修改文件
> 2. `git add .` (全部装箱)
> 3. `git commit -m "更新了什么"` (封箱存档)
> 4. `git push` (一键发射到云端！)

---

## 🌿 5. 分支管理（多线开发）

分支是 Git 中非常强大的功能，允许你在不影响主代码的情况下进行开发。

*   **`git branch`**
    *   **作用**：查看所有分支。
    *   **解释**：列出本地仓库中的所有分支，当前所在分支会有特殊标记。

*   **`git branch <分支名>`**
    *   **作用**：创建新分支。
    *   **解释**：基于当前分支创建一个新的分支，新分支会包含当前分支的所有内容。

*   **`git checkout <分支名>`**
    *   **作用**：切换到指定分支。
    *   **解释**：从当前分支切换到目标分支，工作目录会更新为目标分支的内容。

*   **`git merge <分支名>`**
    *   **作用**：将指定分支合并到当前分支。
    *   **解释**：把目标分支的修改合并到当前分支，常用于功能开发完成后将代码合并回主分支。

*   **`git branch -d <分支名>`**
    *   **作用**：删除指定分支。
    *   **解释**：当分支的工作已经完成并合并后，可以删除该分支以保持仓库整洁。

---

## 🌐 6. 远程操作进阶

除了基础的推送操作，Git 还提供了更多与远程仓库交互的命令。

*   **`git clone <远程仓库地址>`**
    *   **作用**：克隆远程仓库到本地。
    *   **解释**：将远程仓库的所有内容复制到本地，创建一个新的本地仓库。

*   **`git pull`**
    *   **作用**：从远程仓库拉取更新并合并到本地。
    *   **解释**：相当于 `git fetch` 后接 `git merge`，用于获取远程仓库的最新修改。

*   **`git fetch`**
    *   **作用**：从远程仓库获取更新但不合并。
    *   **解释**：只下载远程仓库的最新信息，不自动合并到本地分支，便于查看远程修改。

*   **`git remote -v`**
    *   **作用**：查看远程仓库信息。
    *   **解释**：显示本地仓库关联的所有远程仓库地址及其别名。

---

## 🏷️ 7. 标签管理（版本标记）

标签用于为代码库的特定版本创建快照，常用于发布版本。

*   **`git tag`**
    *   **作用**：查看所有标签。
    *   **解释**：列出仓库中所有的标签。

*   **`git tag <标签名>`**
    *   **作用**：创建轻量级标签。
    *   **解释**：为当前提交创建一个简单的标签，没有额外信息。

*   **`git tag -a <标签名> -m "标签说明"`**
    *   **作用**：创建带注释的标签。
    *   **解释**：为当前提交创建一个带有描述信息的标签，更适合用于版本发布。

*   **`git push --tags`**
    *   **作用**：推送所有标签到远程仓库。
    *   **解释**：将本地创建的标签推送到远程仓库，让团队成员也能看到这些标签。

---

## ⚡ 8. 高级操作（提升效率）

这些命令可以帮助你更高效地管理代码和解决问题。

*   **`git stash`**
    *   **作用**：暂存当前未提交的修改。
    *   **解释**：将当前工作目录的修改暂时保存起来，以便切换到其他分支或进行其他操作。

*   **`git stash pop`**
    *   **作用**：恢复最近一次暂存的修改。
    *   **解释**：将最近一次 `git stash` 保存的修改恢复到工作目录。

*   **`git cherry-pick <提交哈希>`**
    *   **作用**：将指定提交应用到当前分支。
    *   **解释**：从其他分支中选择特定的提交并应用到当前分支，常用于只需要部分修改的场景。

*   **`git rebase <分支名>`**
    *   **作用**：将当前分支的修改重新应用到指定分支上。
    *   **解释**：用于整理提交历史，使分支历史更加清晰线性。注意：不要在已推送到远程的分支上使用。
